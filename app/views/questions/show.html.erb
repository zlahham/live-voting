<%= @question.content %>
<% if @question.choices.any? %>
  <% @question.choices.each do |choice| %>
    <%= choice.content %>
  <% end %>
<% end %>

<%= link_to 'Add Choice', new_question_choice_path(@question) %>
<%= link_to "Back to #{@question.event.title}", event_path(@question.event.id) %>
