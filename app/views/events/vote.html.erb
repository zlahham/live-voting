
<h1><%= @event.title %></h1>

<!-- <ul></ul> -->

<h1 id="testing">FAILING</h1>


<section class="space-50"> </section>

<div class="container question">
  <div class="col-sm-6">
        <h2 id="question-title">Question 1 of 3:</h2>
    <h1>Do you like bananas?</h1>
    <form action="" class="question-form">
      <input type="radio" name="answer" value="1"> Yes, they are great!<br>
      <input type="radio" name="answer" value="2"> No, not sure about them <br>
    </form>
    <button type="submit" class="btn btn-fill btn-primary hundred-width">Submit Answer</button>
  </div>
</div>

<!--
<%= javascript_include_tag "pusher-2.2.3" %>
<script>
  window.addEventListener("DOMContentLoaded", function() {
    <% if defined?(PusherFake) %>
      Pusher.instance = <%== PusherFake.javascript %>;
    <% else %>
      // Other environments, such as production.
    <% end %>
    var
    Chat = Pusher.instance.subscribe("chat");
    Chat.bind("message", function(message) {
      var list = document.getElementsByTagName("ul")[0],
          item = document.createElement("li"),
          text = document.createTextNode(message.body);
      item.appendChild(text);
      list.appendChild(item);
    });
  }, false);
</script> -->
